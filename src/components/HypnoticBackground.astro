---
// Props für Partikelanzahl, Farbe, Interaktivität etc.
---

<div class="particle-container">
    <canvas id="particle-canvas"></canvas>
  </div>
  
  <script is:inline>
    const canvas = document.getElementById('particle-canvas');
    const ctx = canvas.getContext('2d');
    let particles = [];
    let mouse = { x: null, y: null };
  
    function resizeCanvas() {
      canvas.width = window.innerWidth;
      canvas.height = window.innerHeight;
    }
  
    function initParticles() {
      // Partikel-Array erstellen (Position, Geschwindigkeit, Farbe etc.)
      particles = [];
      const numberOfParticles = (canvas.width * canvas.height) / 9000; // Beispiel: Dichte anpassen
      for (let i = 0; i < numberOfParticles; i++) {
          // ... Logik zum Erstellen eines Partikel-Objekts ...
          // particles.push(new Particle( ... ));
      }
    }
  
    function handleMouseMove(event) {
       mouse.x = event.clientX;
       mouse.y = event.clientY;
    }
  
    function animate() {
      ctx.clearRect(0, 0, canvas.width, canvas.height);
  
      particles.forEach(particle => {
        // Partikelposition updaten
        // particle.update(mouse); // Optional: Interaktion mit Maus
  
        // Partikel zeichnen
        // particle.draw(ctx);
  
        // Optional: Linien zwischen nahen Partikeln zeichnen
        // connectParticles(particle, particles, ctx);
      });
  
      requestAnimationFrame(animate);
    }
  
    // Event Listeners
    window.addEventListener('resize', () => {
        resizeCanvas();
        initParticles(); // Neu initialisieren bei Größenänderung
    });
    window.addEventListener('mousemove', handleMouseMove);
  
    // Initialisierung
    resizeCanvas();
    initParticles();
    animate();
  
    // Klassen für Partikel, Funktionen für Linien etc. müssten hier definiert werden
    // class Particle { ... }
    // function connectParticles(...) { ... }
  
  </script>
  
  <style>
  .particle-container {
    position: absolute;
    inset: 0;
    z-index: -1;
    overflow: hidden;
  }
  #particle-canvas {
    display: block; /* Verhindert kleinen Abstand unter dem Canvas */
  }
  </style>